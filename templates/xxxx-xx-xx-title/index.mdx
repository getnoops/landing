---
slug: the-slug
title: The Title
description: The description
image: ./an-image.svg
authors: [someone]
tags: [something, something else]
draft: true
---

{/* truncate */}

Notes:

- Only header levels `##` and `###` appear in the right side bar.
- Even `.md` files are processed as `.mdx`, so it's easier to use `.mdx` (so shortcuts like 'comment code' work correctly).
